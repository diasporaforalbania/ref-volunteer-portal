name: CI Pipeline

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  validate:
    name: Typecheck & Build Validation
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Code
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: 'npm'

      - name: Install Dependencies
        run: npm ci

      - name: TypeScript Typecheck
        run: npm run typecheck

      - name: Run Secure Bridge & CORS Tests
        run: npm run test:bridge

      - name: Vite Production Build
        run: npm run build

      - name: Verify Cloudflare Functions Syntax
        run: |
          node -c functions/api/count.js
          node -c functions/api/send-push.js
